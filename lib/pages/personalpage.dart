import 'package:flutter/material.dart';

class Personalpage extends StatelessWidget {
  const Personalpage({Key key}) : super(key: key);

  Widget build(BuildContext context) {
    return Container(child: Text("personal page"));
  }
}
